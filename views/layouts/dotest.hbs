<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="../styles/dotestpage.css"/>
    <link rel="stylesheet" href=".././styles/colors.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .container {
            padding: 0%;
            margin: 0%;
            max-width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            overflow-x: hidden;
            overflow-y: hidden;
        }

        .middle { 
            flex-grow: 1;
            width: 100%;
            max-height: 80%;  
            padding: 0%;
        }

        .header{
            height: 100%; 
            padding: 0.5% 0.5%; 
            display: flex; 
            flex-direction: row; 
            align-items: center;
        }

        a {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

        .offcanvas-bottom {
            top: auto;
            bottom: 10%;
            height: 50%;
            max-height: 50%;
            width: 50%;
            background-color: var(--vocab-offcanvasbottom-color);
            border-radius: 12px 12px 0% 0%;
            border-color: transparent;
            overflow-y: auto;
        }

        .offcanvas-container {
            width: 49.95%; 
            max-width: 49.95%;
        }

        .offcanvas-backdrop {
            display: none;
        }

        hr {
            border: 0;
            border-top: 1px solid black; 
            margin: 1% 0; 
        }

        h5, p{
            margin: 0%;
            padding: 0%;
            vertical-align: middle;
        }

        .fas {
            color: var(--finger-hand-icon-click-color);
        }

        @media only screen and (max-width: 992px) {
            .col-12 {
                border-right: none !important; 
                border-bottom: 1px solid black; 
                height: 50%;
            }

            .offcanvas-bottom {
                height: 50%;
                max-height: 50%;
                width: 100%;
                overflow-y: auto;
            }

            .offcanvas-container{
                width: 100%;
                max-width: 100%;
            }
        }

        @media only screen and (min-width: 992px){
            .col-lg-6 {
                border-bottom: none !important; 
                border-right: 1px solid black; 
                height: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        {{> header-test}}
        {{{ body }}}
        {{> footer-test
            button1= '<button class="button-footer" data-bs-toggle="modal" data-bs-target="#ExitModal">Thoát</button>'
            button2= '<button class="button-footer" data-bs-toggle="modal" data-bs-target="#FinishModal">Nộp bài</button>'
        }}
        {{> modal 
                modalId="FinishModal"
                modalLabelId="modalLabel"
                modalTitle="Hoàn thành bài tập"
                modalBody="Bạn có chắc chắn nộp phần bài tập đã làm chưa? Nếu chắc chắn với các đáp án đã làm hãy bấm nút hoàn thành"
                modalFooter='
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Quay lại</button>
                    <button type="button" id="btn_submit_test" class="btn btn-primary" onclick="window.location.href=\'/quiz-result\'">Hoàn thành</button>
                '
        }}
        {{> modal 
            modalId="ExitModal"
            modalLabelId="modalLabel"
            modalTitle="Thoát?"
            modalBody="Thay đổi của bạn có thể chưa được lưu. Bạn có chắc chắn muốn thoát bài làm"
            modalFooter='
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Quay lại</button>
                <button type="button" class="btn btn-primary" onclick="window.location.href=\'/filter\'">Thoát</button>
            '
        }}
    </div>
</body>
</html>