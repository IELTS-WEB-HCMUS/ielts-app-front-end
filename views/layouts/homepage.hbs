<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title }}</title>
</head>

<body>
  {{#if name}}
  {{> userloginnavi name=name avatar=avatar}}
  {{else}}
  {{> header }}
  {{/if}}
  {{{ body }}}
  {{> near_footer }}
  {{> footer }}
</body>